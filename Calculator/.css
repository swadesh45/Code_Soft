body {
    background: linear-gradient(to right, #1e726a, #2a7e98);
    font-family: Arial, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.calculator {
    background-color: white;
    padding: 20px;
    border-radius: 10px;
    width: 260px;
    text-align: center;
}

.calculator h2 {
    margin-bottom: 10px;
}

/* Display */
#display {
    width: 100%;
    height: 40px;
    font-size: 20px;
    margin-bottom: 15px;
    text-align: right;
    padding-right: 10px;
}

/* Buttons Grid */
.buttons {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
}

button {
    padding: 15px;
    font-size: 18px;
    border: none;
    background-color: #2a5298;
    color: white;
    border-radius: 5px;
    cursor: pointer;
}

button:hover {
    background-color: #1e3c72;
}

.zero {
    grid-column: span 2;
}